# QA Release Engineer Handoff

Date: 2026-05-26
From: Project Director
To: QA Release Engineer
Subject: first practical slice, instant field capture in Live Report

## Current State

The first practical slice added a quick action strip to Live Report.

Files changed:

- `public/app.php`
- `public/assets/app.js`
- `public/assets/app.css`
- `docs/AI_TEAM/06_CEO_IDEAS_REGISTRY.md`
- AI Team task/status/finding documents

No backend/API/financial formula change was made in this slice.

## Implemented UI

In Live Report note editor:

- `+ Получили`
- `- Наличные`
- `- Карта`
- `Фото`
- `Подотчет`

Expected meaning:

- `+ Получили` starts a cash income line.
- `- Наличные` starts a cash expense line.
- `- Карта` starts a card expense line.
- `Фото` opens the proof picker path.
- `Подотчет` opens `Деньги -> Подотчеты` and must not create an expense.

## Director Verification Already Done

- `node --check public/assets/app.js` passed.
- Local server serves updated `app.php`, `assets/app.js`, and `assets/app.css`.
- Playwright Chromium was installed into local cache.
- Mobile cash screenshot was captured at `/tmp/findesk-quick-capture-mobile.png`.
- Mobile card screenshot was captured at `/tmp/findesk-quick-capture-card-mobile.png`.
- Cash stream quick strip was visible and `+ Получили` inserted a `+` line start.
- Card stream quick strip was visible, cash-only actions were hidden, and `- Карта` inserted a `-` line start.

## Required QA Work

Scenario: quick capture does not break old Live Report behavior.

Device targets:

- mobile `390 x 844`
- tablet `820 x 1180`
- desktop `1440 x 900`

Checks:

1. Open Live Report.
2. Choose `Наличные`.
3. Create a new card.
4. Use `+ Получили`, `- Наличные`, and `Фото`.
5. Save the card.
6. Return to list.
7. Reopen the saved card.
8. Confirm exact rows are restored in view mode.
9. Tap pencil and confirm editing works.
10. Delete the card from inside the card.
11. Confirm the deleted card disappears and user returns to list.
12. Choose `Банковская карта`.
13. Confirm only `- Карта` and `Фото` are shown from money quick actions.
14. Confirm card income is not available.
15. Save and reopen card stream card.
16. Confirm card spending does not affect physical cash totals.
17. Use `Подотчет` from cash stream.
18. Confirm it opens accountable-money/advance workflow and does not create an expense row.
19. Submit cash card out of order if possible.
20. Confirm blocking behavior highlights previous card and shows `Обработайте предыдущую запись в FinDesk.`

## Acceptance

Record results in:

- `FINDINGS.md`
- `TASKS_TO_OTHERS.md` if a failure belongs to another role

Release remains blocked until:

- saved-card reopen is proven;
- delete correctness is proven;
- proof path is proven;
- card/cash separation is proven;
- accountable-money handoff is proven;
- quick capture is proven not to bypass FinDesk review.
